{
    "version": "1.0.0",
    "type": "module",
    "dependencies": {
        "@eviljs/tsconfig": "^1",
        "@eviljs/webpack-config-react": "^1",
        "@types/prop-types": "^15",
        "@types/react-dom": "^17",
        "@types/react": "^17",
        "typescript": "^4.4",
        "webpack-cli": "^4",
        "webpack-dev-server": "^4"
    },
    "scripts": {
        "init": "npm install --global-style",
        "init-vendor": "npm --prefix vendor run init",
        "dev": "NODE_ENV=${NODE_ENV:-development} webpack-dev-server --progress",
        "check": "tsc --noEmit",
        "bundle": "NODE_ENV=production webpack-cli --mode production",
        "build": "npm dedupe --legacy-peer-deps --no-progress --no-fund --no-color && npm run bundle"
    },
    "engines": {
        "node": ">= 14",
        "npm": ">= 7"
    }
}
